[build]
command = "mkdir -p src/environments && echo \"export const environment = { production: true, apiURL: '${API_URL}', movieAPI: '${MOVIE_API}' };\" > src/environments/environment.ts && npm run build"
publish = "dist/todo-list/browser"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
